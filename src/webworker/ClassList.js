import { ThreadCharacter } from "../character/MediatorThread";

/** список классов для паралельного потока
 * @type {Object<string, Class>} 
*/
const _classList = {
    'ThreadCharacter': ThreadCharacter
}

export default _classList;